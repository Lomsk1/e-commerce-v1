// const priceSubmitHandler = () => {
//     priceHandler(priceValue);

// let queryString = "";
// if (!priceMin || !priceMax) {
//   queryString = `${
//     location.search ? `${location.search}&` : "?"
//   }priceMin=${String(priceValue.min)}&priceMax=${String(priceValue.max)}`;

//   // priceHandler(queryString);

//   // navigate(queryString);
// } else {
//   searchParams.set("priceMin", String(priceValue.min));
//   searchParams.set("priceMax", String(priceValue.max));
//   queryString = `?${searchParams.toString()}`;
//   // navigate(`?${searchParams.toString()}`);
//   // priceHandler(queryString);
// }
//   };

// const location = useLocation();
// const navigate = useNavigate();

// const searchParams = new URLSearchParams(location.search);
// const priceMin = searchParams.get("priceMin");
// const priceMax = searchParams.get("priceMax");

{
  /* <ul key={data.id} className="child_ul">
                    {data.brandCategory.map((data) => (
                      <li key={data._id}>
                        <input
                          type="radio"
                          name={"brand"}
                          id={data._id}
                          onChange={(e) => {
                            if (e.target.checked) {
                              brandHandler(data._id);
                            }
                          }}
                        />
                        <label htmlFor={data._id}>{data.name}</label>
                      </li>
                    ))}
                  </ul> */
}
